<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <meta
      name="description"
      content="Sawla General Hospital – compassionate, accessible, and advanced healthcare for the Sawla community and beyond."
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <header class="site-header">
      <div class="top-bar">
        <div class="container top-bar-content">
          <span>24/7 Emergency Care • Call: <a href="tel:+251904575673">+251904575673</a></span>
          <span>Email: <a href="mailto:info@sawlageneralhospital.com">info@sawlageneralhospital.com</a></span>
        </div>
      </div>
      <nav class="navbar" id="navbar">
        <div class="container nav-content">
          <a href="/" class="logo">
            <span class="logo-mark">
              <img src="/images/sawla-logo.png" alt="Sawla General Hospital logo" class="logo-img" />
            </span>
            <span class="logo-text">
              <strong>Sawla General Hospital</strong>
              <small>Compassion. Excellence. Trust.</small>
            </span>
          </a>
          <button class="nav-toggle" id="navToggle" aria-label="Toggle navigation">
            <span></span>
            <span></span>
            <span></span>
          </button>
          <ul class="nav-links" id="navLinks">
            <li><a href="/" class="<%= (title || '').includes('Sawla General Hospital') && !(title || '').includes('|') ? 'active' : '' %>">Home</a></li>
            <li><a href="/about" class="<%= (title || '').includes('About') ? 'active' : '' %>">About</a></li>
            <li><a href="/services" class="<%= (title || '').includes('Services') ? 'active' : '' %>">Services</a></li>
            <li><a href="/departments" class="<%= (title || '').includes('Departments') ? 'active' : '' %>">Departments</a></li>
            <li><a href="/contact" class="<%= (title || '').includes('Contact') ? 'active' : '' %>">Contact</a></li>
            <li>
              <a href="<%= isAdmin ? '/admin/appointments' : '/admin/login' %>" class="admin-link">
                <%= isAdmin ? 'Dashboard' : 'Admin Login' %>
              </a>
            </li>
            <li><a href="#book-appointment" class="btn-primary nav-cta">Book Appointment</a></li>
            <li class="nav-theme">
              <button class="theme-toggle" id="themeToggle" type="button" aria-label="Toggle theme">
                <span class="theme-icon" aria-hidden="true">🌞</span>
                <span class="theme-label">Light</span>
              </button>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <main>


